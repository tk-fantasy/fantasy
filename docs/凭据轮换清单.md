# 🔐 凭据轮换清单（紧急，请立即手动执行）

> 背景：git 提交 `82e41b8` 中的 `config.json.pre_xiaoai_fix` 含多个有效凭据与隐私数据，
> 已推送到 3 个远端（gitee `helloworld21281/aether`、GitHub `tk-fantasy/aether-private`、
> GitHub `tk-fantasy/fantasy`）。第 5 批会重写历史，但**历史重写不能撤销"可能已被人拉取"这件事**，
> 所以以下凭据必须全部轮换——轮换完成后历史里的旧值即作废无害。

## 1. Home Assistant 长期访问令牌（最高优先级）

泄露的令牌有效期到 **2036 年**，持有者可直接控制全屋设备。

- [ ] 打开 HA 管理界面 → 左下角用户头像 → 安全 → 最底部「长期访问令牌」
- [ ] 删除旧令牌（创建于 2026-06 附近、用于 Aether 的那条）
- [ ] 新建长期访问令牌，复制新值
- [ ] 更新 Aether 的 `.env`：`HA_TOKEN=<新令牌>`（同时删掉 `config.json` 里 `ha.token` 明文字段，
      已有 `ha.token_env: "HA_TOKEN"` 机制，明文留着只会扩大泄露面）
- [ ] 重启 aether 容器：`docker compose restart aether`
- [ ] 验证：前端设备页能正常刷新设备列表

## 2. 和风天气 Ed25519 私钥

- [ ] 登录和风天气控制台 → 凭证管理 → 删除项目下的旧 Ed25519 凭证
- [ ] 重新生成（kid/sub 会一起变），更新 `config.json` 的 `weather` 段
- [ ] 验证：聊天里问"今天天气"能正常返回

## 3. 二级密码（全局 LLM Key 门禁）

泄露的是 PBKDF2 哈希，可离线爆破；若密码本身够强风险较低，但轮换零成本：

- [ ] 管理员账号登录 → 模型管理 → 全局 Key 面板 → 重置二级密码
  （或 `DELETE /api/global/password` 后重新设置）

## 4. gitee 仓库可见性确认

- [ ] 检查 `https://gitee.com/helloworld21281/aether` 是否公开可访问；若是私有可保持，
      若公开且本意是开源发布，等第 5 批历史重写完成后再公开

## 5. 历史重写后的收尾（第 5 批完成后）

- [ ] 确认 3 个远端 `git log` 中已搜不到 `config.json.pre_xiaoai_fix`
- [ ] 其他机器的旧 clone 作废，重新 clone（commit hash 全部变化）
- [ ] 摄像头 RTSP 密码、PTZ 密码如与泄露文件中的用户名/环境同名，建议一并改掉
      （泄露文件里是 `*_env` 引用，明文在 .env 未泄露，风险低，属可选加固）
